# XMPP proxy default configuration
#
server
{
    ${core.ipboth.enabled}listen            [::]:5222;
    ${core.ipv4only.enabled}listen          5222;
    ${core.ipv6only.enabled}listen          [::]:5222;
    proxy_pass                              chats-messaging-xmpp;
}
